/**
 * Bytecode analysis implementation for Jarproof.
 *
 * <p>The engine may use only the JDK, ASM, and {@code jarproof-api}. It inspects
 * bytes and metadata without loading analyzed classes.
 */
package sh.zolt.jarproof.engine;
