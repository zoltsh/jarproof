package sh.zolt.jarproof.fixtures.missingclass;

/** Survives both versions so the v2 artifact is not empty. */
public final class ReceiptFormat {
    public String name() {
        return "plain";
    }
}
